<% if posts.any? %>
    <div class="row">
        <% posts.each do |post| %>
            <div class="col-4 image-posts mb-2">
                <%= link_to post_path(post), remote: true do %>
                    <%= attachment_image_tag post, :image, :fill, 200, 200, format: "jpeg", fallback: "no_image.jpg" %>
                <% end %>
            </div>
        <% end %>
    </div>
<% end %>
